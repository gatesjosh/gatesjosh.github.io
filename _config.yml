# Disable the standard theme
# theme: minima 

# Fetch the latest version directly from GitHub
remote_theme: "jekyll/minima"

# Required for remote themes to work on GitHub Pages
plugins:
  - jekyll-remote-theme
  - jekyll-feed
  - jekyll-seo-tag
